# 익명 분석 시스템 설정

이 분석 모듈은 이름, 이메일, IP, 브라우저 정보, 현재 URL을 전송하지 않습니다. 무작위 익명 방문자·세션·플레이 ID만 사용합니다.

- 방문 세션: 30분 동안 활동이 없으면 다음 접속을 재방문으로 기록
- 새로고침: 같은 세션의 방문으로 중복 기록하지 않음
- 완료: 플레이마다 최초 1회
- 공유: 해당 플레이마다 최초 1회
- 브라우저 저장소 삭제, 다른 브라우저 또는 다른 기기 이용 시 새 방문자로 인식

## 1. Google Sheets와 Apps Script 만들기

1. Google Sheets에서 새 스프레드시트를 만듭니다.
2. **확장 프로그램 → Apps Script**를 엽니다.
3. 기본 코드를 모두 지우고 `google-apps-script.gs`의 전체 내용을 붙여넣어 저장합니다.
4. 기존 코드를 교체하는 경우 **배포 관리 → 수정 → 새 버전 → 배포**합니다.
5. Apps Script 편집기에서 `setupDashboard` 함수를 한 번 실행하고 권한을 승인합니다.
6. `Analytics`와 `Dashboard` 시트가 생성됐는지 확인합니다.

`Analytics` 열은 다음 순서로 자동 생성됩니다.

기존 7개 열은 유지되고 `week`, 익명 ID, 재방문·반복 여부, 직역 포함값과 환산 지분 열이 뒤에 추가됩니다.

## 2. Web App 배포

1. Apps Script 우측 상단 **배포 → 새 배포**를 누릅니다.
2. 유형은 **웹 앱**, 실행 사용자는 **나**, 액세스 권한은 **모든 사용자(Anyone)**로 설정합니다.
3. 배포 후 주소가 `/exec`로 끝나는 Web App URL을 복사합니다.
4. `analytics.js` 상단의 `WEB_APP_URL`에 붙여넣습니다.
5. Apps Script 코드를 고친 경우 **배포 관리 → 수정 → 새 버전 → 배포**해야 반영됩니다.

## 3. GitHub Pages 연결

`index.html`에서 게임 스크립트보다 먼저 분석 모듈을 불러옵니다.

```html
<script src="./share-card.js"></script>
<script src="./analytics.js"></script>
<script src="./script.js"></script>
```

변경 파일을 GitHub Pages 브랜치에 올리면 별도 서버 설정 없이 연결됩니다. 배포 후 시크릿/개인 창에서 게임을 한 번 완료하고 Sheets에 `visit`, `complete`, `share` 행이 들어오는지 확인합니다.

## 4. 버전 관리

새 게임을 배포할 때 `analytics.js`의 아래 값만 바꿉니다.

```js
const GAME_VERSION = "1.0.0";
```

버전을 올리면 완료 반복 여부와 통계가 새 버전을 기준으로 다시 집계됩니다. 방문자 ID는 유지되므로 버전 간 재방문도 분석할 수 있습니다.

## 5. 통계 보기

Dashboard는 다음 통계를 생성합니다.

- 순 방문자, 전체 방문 세션, 신규·재방문
- 최초 완료, 전체 플레이, 반복 플레이, 공유와 전환율
- 월별·주별·Version별 통계
- 단독 및 공동 추천 조합 분포
- 직역별 추천 포함 횟수
- 공동 추천을 `1/N`로 나눈 직역별 환산 지분
- Pattern 분포

Google Sheets를 다시 열면 **게임 통계 → Dashboard 새로고침** 메뉴가 생깁니다. 새 데이터가 들어온 뒤 이 메뉴를 누르면 Dashboard가 갱신됩니다.

## 6. 중복 방지와 장애 대응

- localStorage 접두사: `mh-game-analytics-v2`
- 무작위 `visitor_id`, 30분 `session_id`, 매 게임의 `play_id`를 사용합니다.
- 전송이 성공적으로 시작된 뒤에만 해당 세션·플레이 이벤트를 기록 완료로 표시합니다.
- 네트워크 오류가 나면 기록 완료로 표시하지 않아 다음 방문 때 재시도할 수 있습니다.
- 모든 분석 호출은 게임과 분리돼 있으며, 저장소·네트워크·Apps Script 오류가 게임 진행을 중단시키지 않습니다.
- `WEB_APP_URL`이 비어 있으면 분석만 조용히 비활성화되고 게임은 그대로 동작합니다.

분석 기능은 향후 확장될 수 있으므로, 새로운 이벤트나 저장 항목을 쉽게 추가할 수 있는 구조(모듈화된 설계)로 구현했습니다. 또한 관리자 페이지나 대시보드를 추가할 수 있도록 데이터 구조와 함수 인터페이스를 확장 가능하게 설계했습니다.
